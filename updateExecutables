##################### FIRST LINE
# ---------------------------
#!/bin/bash
# ---------------------------
#
# |--------------------------------------------------------------|
# | The script thank you for Notos (notos.korsan@gmail.com)      |
# |--------------------------------------------------------------|
# | The script was further developed Tiby08 (tiby0108@gmail.com) |
# |--------------------------------------------------------------|
#

sudo chmod +x /etc/bestbox-seedbox/updateExecutables
sudo chmod +x /etc/bestbox-seedbox/createSeedboxUser
sudo chmod +x /etc/bestbox-seedbox/deleteSeedboxUser
sudo chmod +x /etc/bestbox-seedbox/installOpenVPN
sudo chmod +x /etc/bestbox-seedbox/removeWebmin
sudo chmod +x /etc/bestbox-seedbox/installWebmin
sudo chmod +x /etc/bestbox-seedbox/downgradeRTorrent
sudo chmod +x /etc/bestbox-seedbox/upgradeRTorrent
sudo chmod +x /etc/bestbox-seedbox/ovpni
sudo chmod +x /etc/bestbox-seedbox/installSABnzbd
sudo chmod +x /etc/bestbox-seedbox/updategitRepository
sudo chmod +x /etc/bestbox-seedbox/createOpenSSLServiceCertificate
sudo chmod +x /etc/bestbox-seedbox/createOpenSSLCACertificate
sudo chmod +x /etc/bestbox-seedbox/upgradeSeedbox
sudo chmod +x /etc/bestbox-seedbox/installRapidleech
sudo chmod +x /etc/bestbox-seedbox/changeUserPassword

##################### LAST LINE ###########
